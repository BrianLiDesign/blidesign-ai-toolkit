# Issue tracker

Issues live in GitHub Issues for the repository's configured `origin` remote. Use the `gh` CLI for
read and write operations after confirming authentication and the target repository. Pull requests
are not an intake surface for triage unless this document is explicitly changed.
